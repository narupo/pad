{@
    from "samples/kokun/templates.pad" import (
        home as homeTemplate,
        about as aboutTemplate,
    )

    def home(request):
        context = {}
        homeTemplate(context)
    end

    def about(request):
        context = []
        aboutTemplate(context)
    end
@}